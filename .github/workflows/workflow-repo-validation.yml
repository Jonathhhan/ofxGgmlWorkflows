name: workflow-repo-validation

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate workflow repository
        shell: pwsh
        run: ./scripts/validate-local.ps1
