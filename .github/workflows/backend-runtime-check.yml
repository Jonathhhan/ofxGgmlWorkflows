name: reusable-backend-runtime-check

on:
  workflow_call:

jobs:
  backend-runtime-check:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Environment info
        shell: bash
        run: |
          echo "Runner: $RUNNER_OS"
          echo "Backend runtime verification scaffold"

      - name: Verify runtime smoke source exists
        shell: bash
        run: |
          test -f tools/runtime-smoke/runtime_smoke.cpp || echo "Runtime smoke source missing"

      - name: CPU backend placeholder
        shell: bash
        run: |
          echo "Future CPU backend initialization build/run"

      - name: CUDA backend placeholder
        shell: bash
        run: |
          echo "Future CUDA runtime discovery"

      - name: Metal backend placeholder
        shell: bash
        run: |
          echo "Future Metal runtime discovery"

      - name: Vulkan backend placeholder
        shell: bash
        run: |
          echo "Future Vulkan runtime discovery"
